-- جدول المناطق الجغرافية
CREATE TABLE regions (
  id SERIAL PRIMARY KEY,
  name TEXT NOT NULL,
  parent_region_id INTEGER REFERENCES regions(id),
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- إضافة بعض المناطق الأساسية
INSERT INTO regions (name) VALUES 
  ('القاهرة'),
  ('الإسكندرية'),
  ('الجيزة'),
  ('المنصورة'),
  ('شبرا الخيمة');


  -- جدول المخازن
CREATE TABLE warehouses (
  id SERIAL PRIMARY KEY,
  name TEXT NOT NULL,
  location TEXT,
  region_id INTEGER REFERENCES regions(id),
  capacity DECIMAL(10,2),
  current_stock DECIMAL(10,2) DEFAULT 0,
  manager_name TEXT,
  contact_phone TEXT,
  is_active BOOLEAN DEFAULT TRUE,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- سياسة الحماية للمخازن
ALTER TABLE warehouses ENABLE ROW LEVEL SECURITY;
CREATE POLICY "المخازن مرئية للمستخدمين المصرح لهم" ON warehouses FOR SELECT USING (true);
CREATE POLICY "المخازن قابلة للتعديل من المسؤولين" ON warehouses FOR INSERT WITH CHECK (true);
CREATE POLICY "المخازن قابلة للتحديث من المسؤولين" ON warehouses FOR UPDATE USING (true);



-- جدول مخزون المواد
CREATE TABLE warehouse_inventory (
  id SERIAL PRIMARY KEY,
  warehouse_id INTEGER REFERENCES warehouses(id),
  category_id UUID REFERENCES categories(id),
  subcategory_id UUID REFERENCES subcategories(id),
  product_id UUID REFERENCES waste_data_admin(id),
  quantity DECIMAL(10,2) DEFAULT 0,
  unit TEXT,
  min_level DECIMAL(10,2),
  max_level DECIMAL(10,2),
  last_updated TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  UNIQUE(warehouse_id, product_id)
);

-- سياسة الحماية لمخزون المواد
ALTER TABLE warehouse_inventory ENABLE ROW LEVEL SECURITY;
CREATE POLICY "مخزون المواد مرئي للمستخدمين المصرح لهم" ON warehouse_inventory FOR SELECT USING (true);
CREATE POLICY "مخزون المواد قابل للتعديل من المسؤولين" ON warehouse_inventory FOR INSERT WITH CHECK (true);
CREATE POLICY "مخزون المواد قابل للتحديث من المسؤولين" ON warehouse_inventory FOR UPDATE USING (true);



-- دالة تحديث المخزون عند تسجيل حركة جديدة
CREATE OR REPLACE FUNCTION update_inventory_on_movement()
RETURNS TRIGGER AS $$
BEGIN
  IF NEW.movement_type = 'in' THEN
    -- إضافة للمخزون
    INSERT INTO warehouse_inventory 
      (warehouse_id, product_id, category_id, subcategory_id, quantity, unit)
    VALUES 
      (NEW.warehouse_id, NEW.product_id, NEW.category_id, NEW.subcategory_id, NEW.quantity, NEW.unit)
    ON CONFLICT (warehouse_id, product_id) 
    DO UPDATE SET
      quantity = warehouse_inventory.quantity + NEW.quantity,
      last_updated = NOW();
  ELSIF NEW.movement_type = 'out' THEN
    -- خصم من المخزون
    UPDATE warehouse_inventory
    SET 
      quantity = GREATEST(0, quantity - NEW.quantity),
      last_updated = NOW()
    WHERE 
      warehouse_id = NEW.warehouse_id AND product_id = NEW.product_id;
  END IF;
  
  -- تحديث إجمالي المخزون في المخزن
  UPDATE warehouses
  SET 
    current_stock = (
      SELECT COALESCE(SUM(quantity), 0)
      FROM warehouse_inventory
      WHERE warehouse_id = NEW.warehouse_id
    ),
    updated_at = NOW()
  WHERE id = NEW.warehouse_id;
  
  RETURN NEW;
END;
$$ LANGUAGE plpgsql;

-- إنشاء الـ Trigger
CREATE TRIGGER trg_update_inventory
AFTER INSERT ON inventory_movements
FOR EACH ROW
EXECUTE FUNCTION update_inventory_on_movement();


----------------------------------------------------------------------نهاية المخازن -----------------------------------------



-- جدول الموردين
CREATE TABLE suppliers (
  id SERIAL PRIMARY KEY,
  name TEXT NOT NULL,
  region_id INTEGER REFERENCES regions(id),
  business_type TEXT,
  contact_person TEXT,
  contact_phone TEXT,
  email TEXT,
  rating DECIMAL(2,1) DEFAULT 0,
  is_active BOOLEAN DEFAULT TRUE,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- سياسة الحماية للموردين
ALTER TABLE suppliers ENABLE ROW LEVEL SECURITY;
CREATE POLICY "الموردين مرئيين للمستخدمين" ON suppliers FOR SELECT USING (true);
CREATE POLICY "الموردين قابلين للتعديل من المسؤولين" ON suppliers FOR INSERT WITH CHECK (true);
CREATE POLICY "الموردين قابلين للتحديث من المسؤولين" ON suppliers FOR UPDATE USING (true);


-- جدول عروض الأسعار من الموردين
CREATE TABLE supplier_price_offers (
  id SERIAL PRIMARY KEY,
  supplier_id INTEGER REFERENCES suppliers(id) NOT NULL,
  category_id UUID REFERENCES categories(id) NOT NULL,
  subcategory_id UUID REFERENCES subcategories(id) NOT NULL,
  product_id UUID REFERENCES waste_data_admin(id) NOT NULL,
  proposed_price DECIMAL(10,2) NOT NULL,
  quantity DECIMAL(10,2) NOT NULL,
  unit TEXT NOT NULL,
  valid_from DATE NOT NULL,
  valid_to DATE NOT NULL,
  status TEXT CHECK (status IN ('pending', 'active', 'rejected', 'expired', 'accepted')) DEFAULT 'pending',
  notes TEXT,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- سياسة الحماية لعروض الأسعار
ALTER TABLE supplier_price_offers ENABLE ROW LEVEL SECURITY;
CREATE POLICY "عروض الأسعار مرئية للمستخدمين" ON supplier_price_offers FOR SELECT USING (true);
CREATE POLICY "عروض الأسعار قابلة للتعديل من المسؤولين" ON supplier_price_offers FOR INSERT WITH CHECK (true);
CREATE POLICY "عروض الأسعار قابلة للتحديث من المسؤولين" ON supplier_price_offers FOR UPDATE USING (true);



---------------------------------------- جداول الموردين نهاية------------------------------------


-- جدول البورصة
CREATE TABLE stock_exchange (
  id SERIAL PRIMARY KEY,
  category_id UUID REFERENCES categories(id) NOT NULL,
  subcategory_id UUID REFERENCES subcategories(id) NOT NULL,
  product_id UUID REFERENCES waste_data_admin(id) NOT NULL,
  region_id INTEGER REFERENCES regions(id) NOT NULL,
  base_price DECIMAL(10,2) NOT NULL,
  buy_price DECIMAL(10,2) NOT NULL,  -- سعر الشراء من العميل
  sell_price DECIMAL(10,2) NOT NULL,  -- سعر البيع للمورد
  demand_level TEXT CHECK (demand_level IN ('low', 'normal', 'high', 'critical')),
  supply_level TEXT CHECK (supply_level IN ('low', 'normal', 'high', 'over_supplied')),
  volatility_index DECIMAL(5,2) DEFAULT 0,  -- مؤشر تقلب السعر
  price_change_percentage DECIMAL(5,2) DEFAULT 0,
  auto_update_enabled BOOLEAN DEFAULT TRUE,
  last_update TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  next_update TIMESTAMP WITH TIME ZONE,
  UNIQUE(product_id, region_id)
);

-- سياسة الحماية للبورصة
ALTER TABLE stock_exchange ENABLE ROW LEVEL SECURITY;
CREATE POLICY "البورصة مرئية للجميع" ON stock_exchange FOR SELECT USING (true);
CREATE POLICY "البورصة قابلة للتعديل من المسؤولين" ON stock_exchange FOR INSERT WITH CHECK (true);
CREATE POLICY "البورصة قابلة للتحديث من المسؤولين" ON stock_exchange FOR UPDATE USING (true);


--------------------------- جدول التسعير الديناميكى للبورصة نهاية ---------------
-- جدول التنبيهات
CREATE TABLE system_alerts (
  id SERIAL PRIMARY KEY,
  alert_type TEXT CHECK (alert_type IN ('stock_low', 'supplier_offer', 'price_change', 'demand_high', 'supply_low', 'warehouse_capacity')),
  severity TEXT CHECK (severity IN ('low', 'medium', 'high', 'critical')),
  message TEXT NOT NULL,
  region_id INTEGER REFERENCES regions(id),
  warehouse_id INTEGER REFERENCES warehouses(id),
  category_id UUID REFERENCES categories(id),
  subcategory_id UUID REFERENCES subcategories(id),
  product_id UUID REFERENCES waste_data_admin(id),
  is_read BOOLEAN DEFAULT FALSE,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  resolved_at TIMESTAMP WITH TIME ZONE
);

-- سياسة الحماية للتنبيهات
ALTER TABLE system_alerts ENABLE ROW LEVEL SECURITY;
CREATE POLICY "التنبيهات مرئية للمستخدمين" ON system_alerts FOR SELECT USING (true);
CREATE POLICY "التنبيهات قابلة للتعديل" ON system_alerts FOR INSERT WITH CHECK (true);
CREATE POLICY "التنبيهات قابلة للتحديث" ON system_alerts FOR UPDATE USING (true);


-- دالة لتحديث أسعار البورصة تلقائيًا
CREATE OR REPLACE FUNCTION update_exchange_prices()
RETURNS void AS $$
DECLARE
  product_rec RECORD;
  supply_level TEXT;
  demand_level TEXT;
  new_buy_price DECIMAL(10,2);
  new_sell_price DECIMAL(10,2);
BEGIN
  -- جلب كل المنتجات في البورصة
  FOR product_rec IN SELECT * FROM stock_exchange WHERE auto_update_enabled = TRUE
  LOOP
    -- حساب مستوى العرض (المخزون)
    SELECT 
      CASE 
        WHEN SUM(wi.quantity) < 200 THEN 'low'
        WHEN SUM(wi.quantity) < 500 THEN 'normal'
        WHEN SUM(wi.quantity) < 1000 THEN 'high'
        ELSE 'over_supplied'
      END INTO supply_level
    FROM warehouse_inventory wi 
    JOIN warehouses w ON wi.warehouse_id = w.id
    WHERE wi.product_id = product_rec.product_id 
    AND w.region_id = product_rec.region_id;
    
    -- حساب مستوى الطلب (عروض الموردين)
    SELECT 
      CASE 
        WHEN COUNT(*) = 0 THEN 'low'
        WHEN COUNT(*) < 3 THEN 'normal'
        WHEN COUNT(*) < 5 THEN 'high'
        ELSE 'critical'
      END INTO demand_level
    FROM supplier_price_offers spo
    WHERE spo.product_id = product_rec.product_id 
    AND spo.status = 'active'
    AND current_date BETWEEN spo.valid_from AND spo.valid_to;

    -- حساب السعر الجديد للشراء من العملاء
    new_buy_price := product_rec.base_price;
    
    -- تأثير العرض
    IF supply_level = 'low' THEN 
      new_buy_price := new_buy_price * 1.10;
    ELSIF supply_level = 'over_supplied' THEN
      new_buy_price := new_buy_price * 0.90;
    END IF;
    
    -- تأثير الطلب
    IF demand_level = 'high' OR demand_level = 'critical' THEN
      new_buy_price := new_buy_price * 1.08;
    ELSIF demand_level = 'low' THEN
      new_buy_price := new_buy_price * 0.95;
    END IF;
    
    -- حساب سعر البيع للموردين (هامش ربح 20%)
    new_sell_price := new_buy_price * 1.2;
    
    -- تحديث جدول البورصة
    UPDATE stock_exchange
    SET 
      buy_price = new_buy_price,
      sell_price = new_sell_price,
      supply_level = supply_level,
      demand_level = demand_level,
      price_change_percentage = ((new_buy_price - base_price) / base_price) * 100,
      last_update = NOW(),
      next_update = NOW() + INTERVAL '6 hours'
    WHERE id = product_rec.id;
    
    -- إنشاء تنبيه إذا كان التغيير كبيرًا
    IF ABS((new_buy_price - product_rec.buy_price) / product_rec.buy_price) > 0.1 THEN
      INSERT INTO system_alerts (
        alert_type, severity, message, region_id, category_id, subcategory_id, product_id
      ) VALUES (
        'price_change',
        'medium',
        'تغير سعر المنتج بنسبة ' || 
        ROUND(ABS((new_buy_price - product_rec.buy_price) / product_rec.buy_price) * 100, 1) || 
        '% للمنتج: ' || (SELECT name FROM waste_data_admin WHERE id = product_rec.product_id),
        product_rec.region_id,
        product_rec.category_id,
        product_rec.subcategory_id,
        product_rec.product_id
      );
    END IF;
  END LOOP;
END;
$$ LANGUAGE plpgsql;

-- جدول تاريخ أسعار البورصة
CREATE TABLE exchange_price_history (
  id SERIAL PRIMARY KEY,
  product_id UUID REFERENCES waste_data_admin(id) NOT NULL,
  region_id INTEGER REFERENCES regions(id) NOT NULL,
  buy_price DECIMAL(10,2) NOT NULL,  -- سعر الشراء من العميل
  sell_price DECIMAL(10,2) NOT NULL,  -- سعر البيع للمورد
  demand_level TEXT,
  supply_level TEXT,
  recorded_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- Trigger لتسجيل تاريخ تغيرات الأسعار
CREATE OR REPLACE FUNCTION record_price_history()
RETURNS TRIGGER AS $$
BEGIN
  IF (OLD.buy_price <> NEW.buy_price OR OLD.sell_price <> NEW.sell_price) THEN
    INSERT INTO exchange_price_history (
      product_id, region_id, buy_price, sell_price, demand_level, supply_level
    ) VALUES (
      NEW.product_id, NEW.region_id, NEW.buy_price, NEW.sell_price, NEW.demand_level, NEW.supply_level
    );
  END IF;
  RETURN NEW;
END;
$$ LANGUAGE plpgsql;

CREATE TRIGGER trg_record_price_history
AFTER UPDATE ON stock_exchange
FOR EACH ROW
EXECUTE FUNCTION record_price_history();


-- جدول تصنيفات جودة المنتجات
CREATE TABLE product_quality_grades (
  id SERIAL PRIMARY KEY,
  product_id UUID REFERENCES waste_data_admin(id) NOT NULL,
  grade TEXT CHECK (grade IN ('A', 'B', 'C', 'D')) NOT NULL,
  grade_description TEXT,
  price_multiplier DECIMAL(3,2) NOT NULL,  -- معامل التأثير على السعر
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  UNIQUE(product_id, grade)
);

-- إضافة بعض البيانات الأساسية
INSERT INTO product_quality_grades (product_id, grade, grade_description, price_multiplier)
SELECT 
  id, 'A', 'ممتاز - خالٍ من الشوائب', 1.08
FROM 
  waste_data_admin 
LIMIT 1;

INSERT INTO product_quality_grades (product_id, grade, grade_description, price_multiplier)
SELECT 
  id, 'B', 'جيد جداً - شوائب قليلة', 1.00
FROM 
  waste_data_admin 
LIMIT 1;

INSERT INTO product_quality_grades (product_id, grade, grade_description, price_multiplier)
SELECT 
  id, 'C', 'متوسط - بعض الشوائب', 0.90
FROM 
  waste_data_admin 
LIMIT 1;


-- جدول إعدادات البورصة
CREATE TABLE exchange_settings (
  id SERIAL PRIMARY KEY,
  setting_key TEXT UNIQUE NOT NULL,
  setting_value TEXT NOT NULL,
  description TEXT,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);

-- إضافة بعض الإعدادات الافتراضية
INSERT INTO exchange_settings (setting_key, setting_value, description) VALUES
  ('price_update_interval', '6', 'الفترة الزمنية لتحديث الأسعار بالساعات'),
  ('max_price_increase', '20', 'الحد الأقصى للزيادة في السعر بالنسبة المئوية'),
  ('min_price_decrease', '20', 'الحد الأقصى للانخفاض في السعر بالنسبة المئوية'),
  ('default_profit_margin', '20', 'هامش الربح الافتراضي بالنسبة المئوية'),
  ('critical_stock_threshold', '200', 'حد المخزون الحرج بالكيلوغرام');



  -- جدول الجدولة الزمنية للموردين
CREATE TABLE supplier_schedules (
  id SERIAL PRIMARY KEY,
  supplier_id INTEGER REFERENCES suppliers(id) NOT NULL,
  warehouse_id INTEGER REFERENCES warehouses(id) NOT NULL,
  scheduled_date DATE NOT NULL,
  scheduled_time TIME,
  estimated_quantity DECIMAL(10,2),
  purpose TEXT CHECK (purpose IN ('delivery', 'pickup', 'meeting')),
  status TEXT CHECK (status IN ('scheduled', 'completed', 'cancelled', 'rescheduled')) DEFAULT 'scheduled',
  notes TEXT,
  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),
  updated_at TIMESTAMP WITH TIME ZONE DEFAULT NOW()
);