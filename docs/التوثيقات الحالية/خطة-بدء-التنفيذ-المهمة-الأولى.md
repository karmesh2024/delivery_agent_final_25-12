# خطة بدء تنفيذ المهمة الأولى: تحليل الوضع الحالي وإعداد البنية التحتية

## المقدمة

هذه الوثيقة تقدم خطة عملية ومفصلة لبدء تنفيذ المهمة الأولى من المرحلة الأولى لمشروع تطوير لوحة التحكم المتكاملة. تتضمن الوثيقة إرشادات مفصلة للفريق وأمثلة عملية وجدول زمني محدد لإنجاز المهمة.

## فريق العمل المطلوب للمهمة الأولى

| الدور | المسؤوليات | المهارات المطلوبة |
|-------|-------------|-------------------|
| مطور واجهة أمامية رئيسي | قيادة تحليل المكونات الحالية وتحديد نمط إعادة الهيكلة | React, TypeScript, Redux |
| مطور خلفية | تحليل التفاعل مع قاعدة البيانات وتحديد هيكل API الموحد | Node.js, Supabase, PostgreSQL |
| مصمم واجهة | تحليل واجهة المستخدم الحالية وتحديد نمط التصميم الموحد | UI/UX, Figma |
| مختبر برمجيات | تحليل الاختبارات الحالية وتحديد استراتيجية الاختبار | Jest, Testing Library |
| مدير المشروع | تنسيق العمل وتوثيق النتائج | إدارة المشاريع, أدوات التوثيق |

## خطوات البدء الفورية

### 1. إنشاء بيئة العمل (اليوم الأول، 8 ساعات)

#### الخطوات العملية:

1. **تجهيز المستودع**:
   ```bash
   # استنساخ المستودع
   git clone https://github.com/your-org/Delivery-Agent-Dashboard.git
   
   # إنشاء فرع جديد للمرحلة الأولى
   git checkout -b phase1/restructure-redux
   
   # تثبيت الاعتماديات
   npm install
   ```

2. **إعداد أدوات التطوير**:
   - تثبيت ESLint مع قواعد TypeScript وReact وRedux
   - تثبيت Prettier لتنسيق الكود
   - إعداد Jest وReact Testing Library للاختبارات
   - إعداد Storybook (اختياري) لتوثيق المكونات

   ```bash
   # تثبيت أدوات التطوير
   npm install --save-dev eslint @typescript-eslint/eslint-plugin @typescript-eslint/parser eslint-plugin-react eslint-plugin-react-hooks @redux-devtools/extension prettier jest @testing-library/react @testing-library/jest-dom @storybook/react
   ```

3. **إعداد إطار العمل للتحليل**:
   - إنشاء مجلد `docs` في جذر المشروع للوثائق
   - إعداد النماذج لتوثيق النطاقات والمكونات
   - إعداد أداة لتتبع التقدم (مثل Trello أو GitHub Projects)

### 2. تحليل النطاقات الحالية (اليوم الأول والثاني، 16 ساعة)

#### الخطوات العملية:

1. **جرد النطاقات والمكونات**:
   - استخدام سكريبت لاستخراج قائمة النطاقات والمكونات الحالية
   
   ```javascript
   // scripts/analyze-domains.js
   const fs = require('fs');
   const path = require('path');
   
   const srcDir = path.join(__dirname, '../src');
   const domainsDir = path.join(srcDir, 'domains');
   
   // قراءة النطاقات الحالية
   const domains = fs.readdirSync(domainsDir, { withFileTypes: true })
       .filter(dirent => dirent.isDirectory())
       .map(dirent => dirent.name);
   
   // تحليل كل نطاق
   domains.forEach(domain => {
       const domainPath = path.join(domainsDir, domain);
       
       // التحقق من وجود شريحة Redux
       const hasReduxSlice = fs.existsSync(path.join(domainPath, 'store'));
       
       // التحقق من وجود واجهة API
       const hasApiLayer = fs.existsSync(path.join(domainPath, 'api'));
       
       // عدد المكونات
       const componentsPath = path.join(domainPath, 'components');
       const componentsCount = fs.existsSync(componentsPath) 
           ? fs.readdirSync(componentsPath).filter(file => file.endsWith('.tsx')).length 
           : 0;
       
       console.log(`Domain: ${domain}`);
       console.log(`  Has Redux Slice: ${hasReduxSlice}`);
       console.log(`  Has API Layer: ${hasApiLayer}`);
       console.log(`  Components Count: ${componentsCount}`);
       console.log('-------------------');
   });
   ```

2. **تحليل استخدام Redux**:
   - تحليل النطاقات التي تستخدم Redux
   - فحص كيفية استخدام Redux في الصفحات المختلفة
   - توثيق الاختلافات في نمط الاستخدام

3. **تحليل التفاعل مع قاعدة البيانات**:
   - فحص آليات التفاعل مع Supabase
   - توثيق الاختلافات في طرق الوصول للبيانات
   - تحديد نقاط الضعف والقوة في الآليات الحالية

4. **إنشاء وثيقة تحليل الوضع الحالي**:
   - توثيق النطاقات والمكونات
   - توثيق نمط استخدام Redux
   - توثيق آليات التفاعل مع قاعدة البيانات
   - توثيق المشاكل والتحديات

#### نموذج وثيقة تحليل الوضع الحالي:

```markdown
# تحليل الوضع الحالي للمشروع

## النطاقات والمكونات

### نطاقات مكتملة التحويل
1. **نطاق الطلبات (Orders)**
   - استخدام Redux: نعم
   - طبقة API موحدة: نعم
   - عدد المكونات: X
   - ملاحظات: ...

2. **نطاق المندوبين (Agents)**
   - استخدام Redux: نعم
   - طبقة API موحدة: نعم
   - عدد المكونات: X
   - ملاحظات: ...

### نطاقات قيد التحويل
1. **نطاق الرحلات (Trips)**
   - استخدام Redux: جزئي
   - طبقة API موحدة: لا
   - عدد المكونات: X
   - ملاحظات: ...

### نطاقات لم تبدأ التحويل
1. **نطاق الرسائل (Messages)**
   - استخدام Redux: لا
   - طبقة API موحدة: لا
   - عدد المكونات: X
   - ملاحظات: ...

## تحليل استخدام Redux

### أنماط الاستخدام المختلفة
- النمط 1: ...
- النمط 2: ...

### مشاكل وتحديات
- مشكلة 1: ...
- مشكلة 2: ...

## تحليل التفاعل مع قاعدة البيانات

### طرق الوصول للبيانات
- الطريقة 1: ...
- الطريقة 2: ...

### مشاكل وتحديات
- مشكلة 1: ...
- مشكلة 2: ...
```

### 3. توحيد أنواع البيانات (اليوم الثالث والرابع، 16 ساعة)

#### الخطوات العملية:

1. **تحليل أنواع البيانات الحالية**:
   - جمع جميع ملفات الأنواع الحالية
   - تحليل التداخل والتكرار بين الأنواع
   - تحديد الأنواع المشتركة بين النطاقات

2. **إنشاء هيكل أنواع البيانات الموحد**:
   - إنشاء مجلد `src/types` للأنواع المشتركة
   - إنشاء ملفات أنواع منفصلة لكل مجال رئيسي

3. **تنفيذ أنواع البيانات الموحدة**:
   
   ```typescript
   // src/types/index.ts
   export * from './customer';
   export * from './agent';
   export * from './order';
   export * from './waste';
   export * from './common';
   export * from './api';
   
   // src/types/customer.ts
   export type CustomerType = 'household' | 'distributor' | 'business' | 'other';
   export type CustomerStatus = 'active' | 'inactive' | 'suspended' | 'blocked';
   export type AddressType = 'home' | 'work' | 'other';
   
   export interface CustomerAddress {
     id: string;
     profileId: string;
     addressType: AddressType;
     addressLine: string;
     city?: string;
     area?: string;
     latitude: number;
     longitude: number;
     isDefault: boolean;
     createdAt: string;
     updatedAt: string;
   }
   
   export interface Customer {
     id: string;
     fullName: string;
     email?: string;
     phoneNumber: string;
     customerType: CustomerType;
     status: CustomerStatus;
     walletId?: string;
     loyaltyPoints: number;
     addresses?: CustomerAddress[];
     createdAt: string;
     updatedAt: string;
   }
   
   // كرر نفس النمط لباقي الأنواع
   ```

4. **تحديث واجهات API الحالية**:
   - تعديل واجهات API الحالية لاستخدام الأنواع الموحدة
   - اختبار التوافق مع الكود الحالي

### 4. إعداد قوالب ومعايير الكود (اليوم الخامس، 8 ساعات)

#### الخطوات العملية:

1. **إعداد قالب مكون Redux Slice**:
   
   ```typescript
   // templates/slice.template.ts
   import { createSlice, createAsyncThunk, PayloadAction } from '@reduxjs/toolkit';
   import { api } from '../api/api';
   import { Entity, EntityState } from '@/types';
   
   interface EntityState {
     items: Entity[];
     selectedItem: Entity | null;
     loading: boolean;
     error: string | null;
   }
   
   const initialState: EntityState = {
     items: [],
     selectedItem: null,
     loading: false,
     error: null,
   };
   
   export const fetchEntities = createAsyncThunk(
     'entity/fetchAll',
     async () => {
       return await api.getAll();
     }
   );
   
   export const fetchEntityById = createAsyncThunk(
     'entity/fetchById',
     async (id: string) => {
       return await api.getById(id);
     }
   );
   
   const entitySlice = createSlice({
     name: 'entity',
     initialState,
     reducers: {
       setSelectedEntity: (state, action: PayloadAction<Entity>) => {
         state.selectedItem = action.payload;
       },
       clearSelectedEntity: (state) => {
         state.selectedItem = null;
       },
     },
     extraReducers: (builder) => {
       builder
         .addCase(fetchEntities.pending, (state) => {
           state.loading = true;
           state.error = null;
         })
         .addCase(fetchEntities.fulfilled, (state, action) => {
           state.loading = false;
           state.items = action.payload;
         })
         .addCase(fetchEntities.rejected, (state, action) => {
           state.loading = false;
           state.error = action.error.message || 'حدث خطأ أثناء جلب البيانات';
         })
         .addCase(fetchEntityById.pending, (state) => {
           state.loading = true;
           state.error = null;
         })
         .addCase(fetchEntityById.fulfilled, (state, action) => {
           state.loading = false;
           state.selectedItem = action.payload;
         })
         .addCase(fetchEntityById.rejected, (state, action) => {
           state.loading = false;
           state.error = action.error.message || 'حدث خطأ أثناء جلب البيانات';
         });
     },
   });
   
   export const { setSelectedEntity, clearSelectedEntity } = entitySlice.actions;
   export default entitySlice.reducer;
   ```

2. **إعداد قالب واجهة API**:
   
   ```typescript
   // templates/api.template.ts
   import { supabase } from '@/lib/supabase';
   import { Entity } from '@/types';
   import { EntityConverter } from '../utils/converters';
   
   export const entityApi = {
     getAll: async (): Promise<Entity[]> => {
       try {
         const { data, error } = await supabase
           .from('entities')
           .select('*')
           .order('created_at', { ascending: false });
         
         if (error) throw error;
         return data?.map(e => EntityConverter.fromDB(e)) || [];
       } catch (error) {
         console.error('Error fetching entities:', error);
         throw error;
       }
     },
   
     getById: async (id: string): Promise<Entity> => {
       try {
         const { data, error } = await supabase
           .from('entities')
           .select('*')
           .eq('id', id)
           .single();
         
         if (error) throw error;
         return EntityConverter.fromDB(data);
       } catch (error) {
         console.error(`Error fetching entity with id ${id}:`, error);
         throw error;
       }
     },
   
     create: async (entity: Omit<Entity, 'id'>): Promise<Entity> => {
       try {
         const { data, error } = await supabase
           .from('entities')
           .insert(EntityConverter.toDB(entity))
           .select()
           .single();
         
         if (error) throw error;
         return EntityConverter.fromDB(data);
       } catch (error) {
         console.error('Error creating entity:', error);
         throw error;
       }
     },
   
     update: async (id: string, entity: Partial<Entity>): Promise<Entity> => {
       try {
         const { data, error } = await supabase
           .from('entities')
           .update(EntityConverter.toDB(entity))
           .eq('id', id)
           .select()
           .single();
         
         if (error) throw error;
         return EntityConverter.fromDB(data);
       } catch (error) {
         console.error(`Error updating entity with id ${id}:`, error);
         throw error;
       }
     },
   
     delete: async (id: string): Promise<void> => {
       try {
         const { error } = await supabase
           .from('entities')
           .delete()
           .eq('id', id);
         
         if (error) throw error;
       } catch (error) {
         console.error(`Error deleting entity with id ${id}:`, error);
         throw error;
       }
     },
   };
   ```

3. **إعداد معايير الترميز**:
   - إعداد ملف ESLint مع قواعد موحدة
   - إعداد ملف Prettier مع تنسيق موحد
   - توثيق معايير الترميز في ملف README

   ```json
   // .eslintrc.json
   {
     "extends": [
       "eslint:recommended",
       "plugin:@typescript-eslint/recommended",
       "plugin:react/recommended",
       "plugin:react-hooks/recommended"
     ],
     "parser": "@typescript-eslint/parser",
     "plugins": ["@typescript-eslint", "react", "react-hooks"],
     "rules": {
       "react/prop-types": "off",
       "react/react-in-jsx-scope": "off",
       "@typescript-eslint/explicit-module-boundary-types": "off",
       "@typescript-eslint/no-explicit-any": "error"
     },
     "settings": {
       "react": {
         "version": "detect"
       }
     }
   }
   ```

## خطة العمل اليومية المفصلة

### اليوم الأول: إعداد البيئة وبدء التحليل

| الوقت | النشاط | المسؤول | المخرجات |
|-------|--------|---------|----------|
| 9:00 - 10:00 | اجتماع بدء المشروع | الفريق بأكمله | فهم مشترك للأهداف |
| 10:00 - 12:00 | إعداد بيئة التطوير | مطور واجهة أمامية + مطور خلفية | بيئة تطوير جاهزة |
| 12:00 - 13:00 | استراحة غداء | - | - |
| 13:00 - 16:00 | بدء تحليل النطاقات والمكونات | الفريق بأكمله | جرد أولي للنطاقات |
| 16:00 - 17:00 | مراجعة اليوم الأول | الفريق بأكمله | خطة اليوم التالي |

### اليوم الثاني: استكمال التحليل

| الوقت | النشاط | المسؤول | المخرجات |
|-------|--------|---------|----------|
| 9:00 - 9:30 | اجتماع يومي | الفريق بأكمله | مراجعة التقدم |
| 9:30 - 12:00 | تحليل استخدام Redux | مطور واجهة أمامية | توثيق أنماط Redux |
| 9:30 - 12:00 | تحليل التفاعل مع قاعدة البيانات | مطور خلفية | توثيق آليات API |
| 12:00 - 13:00 | استراحة غداء | - | - |
| 13:00 - 16:00 | استكمال التحليل | الفريق بأكمله | مسودة وثيقة التحليل |
| 16:00 - 17:00 | مراجعة وتوحيد النتائج | الفريق بأكمله | وثيقة تحليل نهائية |

### اليوم الثالث والرابع: توحيد أنواع البيانات

| الوقت | النشاط | المسؤول | المخرجات |
|-------|--------|---------|----------|
| 9:00 - 9:30 | اجتماع يومي | الفريق بأكمله | مراجعة التقدم |
| 9:30 - 12:00 | تصميم هيكل أنواع البيانات | مطور واجهة أمامية + مطور خلفية | مخطط هيكل الأنواع |
| 12:00 - 13:00 | استراحة غداء | - | - |
| 13:00 - 16:00 | تنفيذ أنواع البيانات | مطور واجهة أمامية + مطور خلفية | ملفات أنواع البيانات |
| 16:00 - 17:00 | مراجعة واختبار | الفريق بأكمله | أنواع بيانات موحدة |

### اليوم الخامس: إعداد القوالب والمعايير

| الوقت | النشاط | المسؤول | المخرجات |
|-------|--------|---------|----------|
| 9:00 - 9:30 | اجتماع يومي | الفريق بأكمله | مراجعة التقدم |
| 9:30 - 12:00 | إعداد قوالب Redux وAPI | مطور واجهة أمامية + مطور خلفية | قوالب جاهزة |
| 12:00 - 13:00 | استراحة غداء | - | - |
| 13:00 - 15:00 | إعداد معايير الترميز | الفريق بأكمله | معايير موثقة |
| 15:00 - 16:30 | توثيق المرحلة الأولى | مدير المشروع | وثائق المرحلة الأولى |
| 16:30 - 17:00 | مراجعة الأسبوع | الفريق بأكمله | خطة الأسبوع التالي |

## المخرجات المتوقعة

1. **وثيقة تحليل الوضع الحالي**
   - تحليل شامل للنطاقات والمكونات
   - تحليل استخدام Redux
   - تحليل آليات التفاعل مع قاعدة البيانات

2. **مكتبة أنواع البيانات الموحدة**
   - ملفات أنواع البيانات لجميع النطاقات
   - توثيق العلاقات بين الأنواع المختلفة

3. **قوالب ومعايير الترميز**
   - قوالب لشرائح Redux
   - قوالب لواجهات API
   - معايير الترميز الموحدة

## خاتمة

تمثل هذه الخطة دليلاً عملياً لبدء تنفيذ المهمة الأولى من المرحلة الأولى للمشروع. من خلال اتباع هذه الخطوات المفصلة، سيتمكن الفريق من إكمال تحليل الوضع الحالي وإعداد البنية التحتية اللازمة لإعادة هيكلة المشروع وتوحيد استخدام Redux.

تحتاج هذه الخطة إلى المراجعة والتحديث المستمر استناداً إلى النتائج والتحديات التي تظهر أثناء التنفيذ. يجب على الفريق الاجتماع بشكل يومي لمراجعة التقدم وتعديل الخطة إذا لزم الأمر.

بمجرد الانتهاء من المهمة الأولى، سيكون الفريق جاهزاً للانتقال إلى المهمة التالية وهي تطوير طبقة API الموحدة، وذلك وفقاً للمرحلة الأولى من خطة التنفيذ الشاملة.