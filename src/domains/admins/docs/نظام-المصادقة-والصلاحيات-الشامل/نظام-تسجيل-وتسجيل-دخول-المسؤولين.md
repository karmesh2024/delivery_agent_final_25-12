# دليل إنشاء نظام تسجيل وتسجيل دخول المسؤولين

هذا الدليل يشرح خطوة بخطوة كيفية إنشاء نظام تسجيل مستخدمين وتسجيل دخول متكامل مع نظام الصلاحيات الذي تم تطويره، باستخدام معمارية DDD (Domain-Driven Design) وRedux.

## المحتويات

1. [إعداد قاعدة البيانات للمصادقة](#1-إعداد-قاعدة-البيانات-للمصادقة)
2. [إنشاء أنواع البيانات والواجهات](#2-إنشاء-أنواع-البيانات-والواجهات)
3. [تطوير واجهة برمجة المصادقة](#3-تطوير-واجهة-برمجة-المصادقة)
4. [إعداد شريحة Redux للمصادقة](#4-إعداد-شريحة-redux-للمصادقة)
5. [إنشاء صفحات المصادقة](#5-إنشاء-صفحات-المصادقة)
6. [تطبيق حراسة الطرق](#6-تطبيق-حراسة-الطرق)
7. [ربط النظام مع إدارة الصلاحيات](#7-ربط-النظام-مع-إدارة-الصلاحيات)
8. [اختبار وتحسين النظام](#8-اختبار-وتحسين-النظام)

## 1. إعداد قاعدة البيانات للمصادقة

### 1.1 التأكد من إعداد Supabase بشكل صحيح

قبل البدء، تأكد من أن Supabase مُعد بشكل صحيح مع تفعيل خدمة المصادقة:

1. قم بالوصول إلى لوحة تحكم Supabase الخاصة بمشروعك
2. انتقل إلى قسم "Authentication" وتأكد من تفعيل خيار Email/Password

### 1.2 إنشاء إجراء SQL لإدارة المستخدمين الجدد

أنشئ ملفًا جديدًا `src/domains/admins/data/admin-auth-triggers.sql`:

```sql
-- إجراء مخصص للتحقق من البريد الإلكتروني والإدخال التلقائي للمسؤولين
CREATE OR REPLACE FUNCTION public.process_new_admin_user()
RETURNS TRIGGER AS $$
DECLARE
  default_role_id uuid;
BEGIN
  -- الحصول على المعرف الافتراضي للدور
  SELECT id INTO default_role_id FROM public.roles WHERE code = 'viewer' LIMIT 1;
  
  -- التحقق من وجود البريد الإلكتروني في قائمة المدعوين (اختياري)
  -- IF NOT EXISTS (SELECT 1 FROM public.admin_invitations WHERE email = NEW.email AND is_used = false) THEN
  --   RETURN NEW;
  -- END IF;
  
  -- إدراج المستخدم في جدول المسؤولين
  INSERT INTO public.admins (
    user_id,
    email,
    username,
    full_name,
    is_active,
    role_id
  ) VALUES (
    NEW.id,
    NEW.email,
    SPLIT_PART(NEW.email, '@', 1), -- استخدام جزء من البريد الإلكتروني كاسم مستخدم افتراضي
    NEW.raw_user_meta_data->>'full_name', -- استخدام الاسم من البيانات الوصفية إن وجد
    true, -- نشط افتراضيًا
    default_role_id
  );
  
  -- تحديث الدعوة إن وجدت (اختياري)
  -- UPDATE public.admin_invitations 
  -- SET is_used = true, used_at = now()
  -- WHERE email = NEW.email AND is_used = false;
  
  RETURN NEW;
END;
$$ LANGUAGE plpgsql SECURITY DEFINER;

-- إنشاء المحفز لتشغيل الإجراء عند إنشاء مستخدم جديد
DROP TRIGGER IF EXISTS on_auth_user_created ON auth.users;
CREATE TRIGGER on_auth_user_created
  AFTER INSERT ON auth.users
  FOR EACH ROW EXECUTE FUNCTION public.process_new_admin_user();

-- إجراء إضافي لتحديث بيانات المسؤول عند تحديث المستخدم (اختياري)
CREATE OR REPLACE FUNCTION public.update_admin_on_user_change()
RETURNS TRIGGER AS $$
BEGIN
  -- تحديث بيانات المسؤول عند تغيير بيانات المستخدم
  UPDATE public.admins
  SET 
    email = NEW.email,
    updated_at = now()
  WHERE user_id = NEW.id;
  
  RETURN NEW;
END;
$$ LANGUAGE plpgsql SECURITY DEFINER;

-- إنشاء المحفز لتشغيل الإجراء عند تحديث مستخدم
DROP TRIGGER IF EXISTS on_auth_user_updated ON auth.users;
CREATE TRIGGER on_auth_user_updated
  AFTER UPDATE ON auth.users
  FOR EACH ROW EXECUTE FUNCTION public.update_admin_on_user_change();
```

نفذ هذا الملف في قاعدة البيانات الخاصة بك. هذا سيعد النظام ليقوم تلقائيًا بإضافة المستخدمين الجدد إلى جدول `admins` عند تسجيلهم.

### 1.3 إعداد جدول دعوات المسؤولين (اختياري)

إذا كنت ترغب في نظام دعوات للمسؤولين، أنشئ الجدول التالي:

```sql
-- جدول دعوات المسؤولين
CREATE TABLE IF NOT EXISTS public.admin_invitations (
  id uuid NOT NULL DEFAULT extensions.uuid_generate_v4(),
  email text NOT NULL,
  role_id uuid REFERENCES public.roles(id),
  token text NOT NULL,
  expires_at timestamptz NOT NULL,
  is_used boolean NOT NULL DEFAULT false,
  used_at timestamptz,
  invited_by uuid REFERENCES public.admins(id),
  created_at timestamptz NOT NULL DEFAULT CURRENT_TIMESTAMP,
  
  CONSTRAINT admin_invitations_pkey PRIMARY KEY (id),
  CONSTRAINT admin_invitations_email_key UNIQUE (email),
  CONSTRAINT admin_invitations_token_key UNIQUE (token)
);

-- فهرس على حالة الاستخدام وتاريخ الانتهاء
CREATE INDEX IF NOT EXISTS idx_admin_invitations_status ON public.admin_invitations(is_used, expires_at);
```

## 2. إنشاء أنواع البيانات والواجهات

### 2.1 تحديث ملف أنواع المصادقة

أنشئ أو قم بتحديث ملف `src/domains/admins/types/auth.ts`:

```typescript
import { Admin, AdminRole } from './index';

export interface AdminCredentials {
  email: string;
  password: string;
}

export interface AdminRegistration {
  email: string;
  password: string;
  confirm_password: string;
  full_name: string;
  invitation_token?: string; // اختياري إذا كنت تستخدم نظام الدعوات
}

export interface AuthState {
  isAuthenticated: boolean;
  currentAdmin: Admin | null;
  token: string | null;
  loading: boolean;
  error: string | null;
  resetEmailSent: boolean;
}

export interface AuthResult {
  success: boolean;
  admin?: Admin;
  token?: string;
  error?: string;
}

export interface ResetPasswordRequest {
  email: string;
}

export interface UpdatePasswordRequest {
  password: string;
  confirm_password: string;
}
```

### 2.2 تحديث ملف نوع المسؤول

تأكد من أن ملف `src/domains/admins/types/index.ts` يحتوي على تعريفات المسؤول المناسبة:

```typescript
export type AdminRole = 'super_admin' | 'admin' | 'manager' | 'supervisor' | 'support' | 'viewer';

export type AdminPermissions = Record<string, boolean>;

export const DEFAULT_ROLE_PERMISSIONS: Record<AdminRole, AdminPermissions> = {
  // تم إنشاؤه سابقًا...
};

export interface Admin {
  id: string;
  user_id: string;
  email: string;
  username?: string;
  full_name?: string;
  is_active: boolean;
  role: AdminRole;
  role_id?: string;
  permissions?: AdminPermissions;
  department_id?: string;
  manager_id?: string;
  phone?: string;
  profile_image_url?: string;
  created_at: string;
  updated_at: string;
}
```

## 3. تطوير واجهة برمجة المصادقة

### 3.1 إنشاء ملف واجهة برمجة المصادقة

أنشئ ملف `src/domains/admins/api/authApi.ts`:

```typescript
import { supabase } from '@/lib/supabase';
import {
  AdminCredentials,
  AdminRegistration,
  AuthResult,
  ResetPasswordRequest,
  UpdatePasswordRequest,
  Admin
} from '../types/auth';
import { DEFAULT_ROLE_PERMISSIONS } from '../types';

/**
 * واجهة برمجة التطبيق للمصادقة
 */
export const authApi = {
  /**
   * تسجيل الدخول للمسؤول
   */
  login: async (credentials: AdminCredentials): Promise<AuthResult> => {
    try {
      // تسجيل الدخول باستخدام Supabase
      const { data: authData, error: authError } = await supabase.auth.signInWithPassword({
        email: credentials.email,
        password: credentials.password,
      });

      if (authError) throw authError;

      // التحقق من وجود المستخدم في جدول المسؤولين
      const { data: adminData, error: adminError } = await supabase
        .from('admins')
        .select(`
          *,
          role:roles(id, name, code)
        `)
        .eq('user_id', authData.user.id)
        .eq('is_active', true)
        .single();

      if (adminError) {
        // إذا لم يتم العثور على المسؤول أو غير نشط
        await supabase.auth.signOut();
        throw new Error('غير مصرح لهذا المستخدم بالوصول إلى لوحة التحكم.');
      }

      // تحويل البيانات إلى كائن Admin
      const admin: Admin = {
        id: adminData.id,
        user_id: adminData.user_id,
        email: adminData.email,
        username: adminData.username,
        full_name: adminData.full_name,
        is_active: adminData.is_active,
        role: adminData.role?.code || 'viewer',
        role_id: adminData.role_id,
        department_id: adminData.department_id,
        manager_id: adminData.manager_id,
        phone: adminData.phone,
        profile_image_url: adminData.profile_image_url,
        permissions: adminData.permissions || DEFAULT_ROLE_PERMISSIONS[adminData.role?.code || 'viewer'],
        created_at: adminData.created_at,
        updated_at: adminData.updated_at
      };

      return {
        success: true,
        admin,
        token: authData.session?.access_token
      };
    } catch (error) {
      console.error('خطأ في تسجيل الدخول:', error);
      return {
        success: false,
        error: error instanceof Error ? error.message : 'حدث خطأ أثناء تسجيل الدخول'
      };
    }
  },

  /**
   * تسجيل مسؤول جديد
   */
  register: async (registration: AdminRegistration): Promise<AuthResult> => {
    try {
      // التحقق من تطابق كلمتي المرور
      if (registration.password !== registration.confirm_password) {
        throw new Error('كلمتا المرور غير متطابقتين');
      }

      // التحقق من رمز الدعوة إذا كان مطلوبًا
      if (registration.invitation_token) {
        const { data: invitation, error: invitationError } = await supabase
          .from('admin_invitations')
          .select('*')
          .eq('token', registration.invitation_token)
          .eq('is_used', false)
          .gt('expires_at', new Date().toISOString())
          .single();

        if (invitationError || !invitation) {
          throw new Error('رمز الدعوة غير صالح أو منتهي الصلاحية');
        }

        if (invitation.email !== registration.email) {
          throw new Error('البريد الإلكتروني لا يتطابق مع الدعوة');
        }
      }

      // إنشاء حساب المستخدم في Supabase
      const { data: authData, error: authError } = await supabase.auth.signUp({
        email: registration.email,
        password: registration.password,
        options: {
          data: {
            full_name: registration.full_name
          }
        }
      });

      if (authError) throw authError;

      // المستخدم تم إنشاؤه، لكن قد يحتاج إلى تأكيد البريد الإلكتروني
      // معالج المحفز الذي أنشأناه سيضيف المستخدم تلقائيًا إلى جدول المسؤولين

      return {
        success: true,
        token: authData.session?.access_token,
        admin: undefined // لن يكون هناك مسؤول حتى يتم تأكيد البريد الإلكتروني
      };
    } catch (error) {
      console.error('خطأ في تسجيل مسؤول جديد:', error);
      return {
        success: false,
        error: error instanceof Error ? error.message : 'حدث خطأ أثناء تسجيل المسؤول الجديد'
      };
    }
  },

  /**
   * تسجيل خروج المسؤول
   */
  logout: async (): Promise<{ success: boolean, error?: string }> => {
    try {
      const { error } = await supabase.auth.signOut();
      if (error) throw error;
      
      return { success: true };
    } catch (error) {
      console.error('خطأ في تسجيل الخروج:', error);
      return {
        success: false,
        error: error instanceof Error ? error.message : 'حدث خطأ أثناء تسجيل الخروج'
      };
    }
  },

  /**
   * التحقق من حالة المصادقة الحالية
   */
  checkAuth: async (): Promise<AuthResult> => {
    try {
      // التحقق من وجود جلسة حالية
      const { data: sessionData, error: sessionError } = await supabase.auth.getSession();
      
      if (sessionError) throw sessionError;
      if (!sessionData.session) {
        return { success: false, error: 'لا توجد جلسة نشطة' };
      }

      // جلب بيانات المسؤول
      const { data: adminData, error: adminError } = await supabase
        .from('admins')
        .select(`
          *,
          role:roles(id, name, code)
        `)
        .eq('user_id', sessionData.session.user.id)
        .eq('is_active', true)
        .single();

      if (adminError) {
        await supabase.auth.signOut();
        throw new Error('غير مصرح لهذا المستخدم بالوصول إلى لوحة التحكم');
      }

      // تحويل البيانات إلى كائن Admin
      const admin: Admin = {
        id: adminData.id,
        user_id: adminData.user_id,
        email: adminData.email,
        username: adminData.username,
        full_name: adminData.full_name,
        is_active: adminData.is_active,
        role: adminData.role?.code || 'viewer',
        role_id: adminData.role_id,
        department_id: adminData.department_id,
        manager_id: adminData.manager_id,
        phone: adminData.phone,
        profile_image_url: adminData.profile_image_url,
        permissions: adminData.permissions || DEFAULT_ROLE_PERMISSIONS[adminData.role?.code || 'viewer'],
        created_at: adminData.created_at,
        updated_at: adminData.updated_at
      };

      return {
        success: true,
        admin,
        token: sessionData.session.access_token
      };
    } catch (error) {
      console.error('خطأ في التحقق من حالة المصادقة:', error);
      return {
        success: false,
        error: error instanceof Error ? error.message : 'حدث خطأ أثناء التحقق من حالة المصادقة'
      };
    }
  },

  /**
   * طلب إعادة تعيين كلمة المرور
   */
  resetPassword: async (request: ResetPasswordRequest): Promise<{ success: boolean, error?: string }> => {
    try {
      const { error } = await supabase.auth.resetPasswordForEmail(request.email);
      if (error) throw error;
      
      return { success: true };
    } catch (error) {
      console.error('خطأ في طلب إعادة تعيين كلمة المرور:', error);
      return {
        success: false,
        error: error instanceof Error ? error.message : 'حدث خطأ أثناء طلب إعادة تعيين كلمة المرور'
      };
    }
  },

  /**
   * تحديث كلمة المرور
   */
  updatePassword: async (request: UpdatePasswordRequest): Promise<{ success: boolean, error?: string }> => {
    try {
      if (request.password !== request.confirm_password) {
        throw new Error('كلمتا المرور غير متطابقتين');
      }

      const { error } = await supabase.auth.updateUser({
        password: request.password
      });

      if (error) throw error;
      
      return { success: true };
    } catch (error) {
      console.error('خطأ في تحديث كلمة المرور:', error);
      return {
        success: false,
        error: error instanceof Error ? error.message : 'حدث خطأ أثناء تحديث كلمة المرور'
      };
    }
  }
};
```

## 4. إعداد شريحة Redux للمصادقة

### 4.1 إنشاء شريحة المصادقة

أنشئ ملف `src/domains/admins/store/authSlice.ts`:

```typescript
import { createSlice, createAsyncThunk, PayloadAction } from '@reduxjs/toolkit';
import { authApi } from '../api/authApi';
import { AuthState, AdminCredentials, AdminRegistration, ResetPasswordRequest, UpdatePasswordRequest } from '../types/auth';

// الحالة الأولية
const initialState: AuthState = {
  isAuthenticated: false,
  currentAdmin: null,
  token: null,
  loading: false,
  error: null,
  resetEmailSent: false
};

// الأحداث (Thunks)
export const login = createAsyncThunk(
  'auth/login',
  async (credentials: AdminCredentials, { rejectWithValue }) => {
    try {
      const result = await authApi.login(credentials);
      if (!result.success) {
        return rejectWithValue(result.error || 'فشل تسجيل الدخول');
      }
      return result;
    } catch (error) {
      return rejectWithValue(error instanceof Error ? error.message : 'حدث خطأ أثناء تسجيل الدخول');
    }
  }
);

export const register = createAsyncThunk(
  'auth/register',
  async (registration: AdminRegistration, { rejectWithValue }) => {
    try {
      const result = await authApi.register(registration);
      if (!result.success) {
        return rejectWithValue(result.error || 'فشل التسجيل');
      }
      return result;
    } catch (error) {
      return rejectWithValue(error instanceof Error ? error.message : 'حدث خطأ أثناء التسجيل');
    }
  }
);

export const logout = createAsyncThunk(
  'auth/logout',
  async (_, { rejectWithValue }) => {
    try {
      const result = await authApi.logout();
      if (!result.success) {
        return rejectWithValue(result.error || 'فشل تسجيل الخروج');
      }
      return result;
    } catch (error) {
      return rejectWithValue(error instanceof Error ? error.message : 'حدث خطأ أثناء تسجيل الخروج');
    }
  }
);

export const checkAuth = createAsyncThunk(
  'auth/checkAuth',
  async (_, { rejectWithValue }) => {
    try {
      const result = await authApi.checkAuth();
      if (!result.success) {
        return rejectWithValue(result.error || 'فشل التحقق من المصادقة');
      }
      return result;
    } catch (error) {
      return rejectWithValue(error instanceof Error ? error.message : 'حدث خطأ أثناء التحقق من المصادقة');
    }
  }
);

export const resetPassword = createAsyncThunk(
  'auth/resetPassword',
  async (request: ResetPasswordRequest, { rejectWithValue }) => {
    try {
      const result = await authApi.resetPassword(request);
      if (!result.success) {
        return rejectWithValue(result.error || 'فشل طلب إعادة تعيين كلمة المرور');
      }
      return result;
    } catch (error) {
      return rejectWithValue(error instanceof Error ? error.message : 'حدث خطأ أثناء طلب إعادة تعيين كلمة المرور');
    }
  }
);

export const updatePassword = createAsyncThunk(
  'auth/updatePassword',
  async (request: UpdatePasswordRequest, { rejectWithValue }) => {
    try {
      const result = await authApi.updatePassword(request);
      if (!result.success) {
        return rejectWithValue(result.error || 'فشل تحديث كلمة المرور');
      }
      return result;
    } catch (error) {
      return rejectWithValue(error instanceof Error ? error.message : 'حدث خطأ أثناء تحديث كلمة المرور');
    }
  }
);

// شريحة Redux
export const authSlice = createSlice({
  name: 'auth',
  initialState,
  reducers: {
    clearError: (state) => {
      state.error = null;
    },
    clearResetEmailSent: (state) => {
      state.resetEmailSent = false;
    }
  },
  extraReducers: (builder) => {
    builder
      // معالجة تسجيل الدخول
      .addCase(login.pending, (state) => {
        state.loading = true;
        state.error = null;
      })
      .addCase(login.fulfilled, (state, action) => {
        state.loading = false;
        state.isAuthenticated = true;
        state.currentAdmin = action.payload.admin!;
        state.token = action.payload.token!;
      })
      .addCase(login.rejected, (state, action) => {
        state.loading = false;
        state.error = action.payload as string;
      })
      
      // معالجة التسجيل
      .addCase(register.pending, (state) => {
        state.loading = true;
        state.error = null;
      })
      .addCase(register.fulfilled, (state) => {
        state.loading = false;
        // لاحظ: لن نقوم بتسجيل الدخول تلقائيًا بعد التسجيل،
        // لأن المستخدم قد يحتاج إلى تأكيد البريد الإلكتروني
      })
      .addCase(register.rejected, (state, action) => {
        state.loading = false;
        state.error = action.payload as string;
      })
      
      // معالجة تسجيل الخروج
      .addCase(logout.pending, (state) => {
        state.loading = true;
      })
      .addCase(logout.fulfilled, (state) => {
        state.loading = false;
        state.isAuthenticated = false;
        state.currentAdmin = null;
        state.token = null;
      })
      .addCase(logout.rejected, (state, action) => {
        state.loading = false;
        state.error = action.payload as string;
      })
      
      // التحقق من المصادقة
      .addCase(checkAuth.pending, (state) => {
        state.loading = true;
      })
      .addCase(checkAuth.fulfilled, (state, action) => {
        state.loading = false;
        state.isAuthenticated = true;
        state.currentAdmin = action.payload.admin!;
        state.token = action.payload.token!;
      })
      .addCase(checkAuth.rejected, (state) => {
        state.loading = false;
        state.isAuthenticated = false;
        state.currentAdmin = null;
        state.token = null;
      })
      
      // إعادة تعيين كلمة المرور
      .addCase(resetPassword.pending, (state) => {
        state.loading = true;
        state.error = null;
        state.resetEmailSent = false;
      })
      .addCase(resetPassword.fulfilled, (state) => {
        state.loading = false;
        state.resetEmailSent = true;
      })
      .addCase(resetPassword.rejected, (state, action) => {
        state.loading = false;
        state.error = action.payload as string;
      })
      
      // تحديث كلمة المرور
      .addCase(updatePassword.pending, (state) => {
        state.loading = true;
        state.error = null;
      })
      .addCase(updatePassword.fulfilled, (state) => {
        state.loading = false;
      })
      .addCase(updatePassword.rejected, (state, action) => {
        state.loading = false;
        state.error = action.payload as string;
      });
  }
});

// تصدير الإجراءات
export const { clearError, clearResetEmailSent } = authSlice.actions;

// تصدير الشريحة
export default authSlice.reducer;
```

### 4.2 تحديث متجر Redux الرئيسي

قم بتحديث ملف `src/store/index.ts` لإضافة شريحة المصادقة:

```typescript
import { configureStore } from '@reduxjs/toolkit';
import authReducer from '../domains/admins/store/authSlice';
import adminsReducer from '../domains/admins/store/adminsSlice';
// استيراد reducers أخرى...

export const store = configureStore({
  reducer: {
    auth: authReducer,
    admins: adminsReducer,
    // reducers أخرى...
  },
  // تكوينات أخرى...
});

export type RootState = ReturnType<typeof store.getState>;
export type AppDispatch = typeof store.dispatch;
```

## 5. إنشاء صفحات المصادقة

### 5.1 صفحة تسجيل الدخول

أنشئ ملف `src/app/login/page.tsx`:

```tsx
'use client';

import { useEffect, useState } from 'react';
import { useRouter } from 'next/navigation';
import { useAppDispatch, useAppSelector } from '@/store/hooks';
import { login, clearError } from '@/domains/admins/store/authSlice';
import Link from 'next/link';
import { Button } from '@/shared/components/ui/button';
import { Input } from '@/shared/components/ui/input';
import { Label } from '@/shared/components/ui/label';
import { Card, CardContent, CardDescription, CardFooter, CardHeader, CardTitle } from '@/shared/components/ui/card';
import { Alert, AlertDescription } from '@/shared/components/ui/alert';

export default function LoginPage() {
  const [email, setEmail] = useState('');
  const [password, setPassword] = useState('');
  const router = useRouter();
  const dispatch = useAppDispatch();
  const { isAuthenticated, loading, error } = useAppSelector((state) => state.auth);
  
  // إعادة التوجيه إذا كان المستخدم مسجل الدخول بالفعل
  useEffect(() => {
    if (isAuthenticated) {
      router.push('/dashboard');
    }
  }, [isAuthenticated, router]);
  
  // تنظيف الخطأ عند التحميل
  useEffect(() => {
    dispatch(clearError());
  }, [dispatch]);
  
  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault();
    dispatch(login({ email, password }));
  };
  
  return (
    <div className="container flex items-center justify-center min-h-screen">
      <Card className="w-full max-w-md">
        <CardHeader>
          <CardTitle className="text-2xl text-center">تسجيل الدخول</CardTitle>
          <CardDescription className="text-center">
            قم بتسجيل الدخول إلى لوحة تحكم المسؤولين
          </CardDescription>
        </CardHeader>
        <CardContent>
          <form onSubmit={handleSubmit} className="space-y-4">
            {error && (
              <Alert variant="destructive">
                <AlertDescription>{error}</AlertDescription>
              </Alert>
            )}
            
            <div className="space-y-2">
              <Label htmlFor="email">البريد الإلكتروني</Label>
              <Input 
                id="email" 
                type="email" 
                value={email}
                onChange={(e) => setEmail(e.target.value)}
                required
              />
            </div>
            
            <div className="space-y-2">
              <div className="flex items-center justify-between">
                <Label htmlFor="password">كلمة المرور</Label>
                <Link 
                  href="/forgot-password" 
                  className="text-sm text-blue-600 hover:text-blue-800"
                >
                  نسيت كلمة المرور؟
                </Link>
              </div>
              <Input 
                id="password" 
                type="password" 
                value={password}
                onChange={(e) => setPassword(e.target.value)}
                required
              />
            </div>
            
            <Button 
              type="submit" 
              className="w-full" 
              disabled={loading}
            >
              {loading ? 'جاري تسجيل الدخول...' : 'تسجيل الدخول'}
            </Button>
          </form>
        </CardContent>
        <CardFooter className="flex justify-center">
          <div className="text-sm text-gray-600">
            ليس لديك حساب؟{' '}
            <Link href="/register" className="text-blue-600 hover:text-blue-800">
              التسجيل
            </Link>
          </div>
        </CardFooter>
      </Card>
    </div>
  );
}
```

### 5.2 صفحة التسجيل

أنشئ ملف `src/app/register/page.tsx`:

```tsx
'use client';

import { useEffect, useState } from 'react';
import { useRouter, useSearchParams } from 'next/navigation';
import { useAppDispatch, useAppSelector } from '@/store/hooks';
import { register, clearError } from '@/domains/admins/store/authSlice';
import Link from 'next/link';
import { Button } from '@/shared/components/ui/button';
import { Input } from '@/shared/components/ui/input';
import { Label } from '@/shared/components/ui/label';
import { Card, CardContent, CardDescription, CardFooter, CardHeader, CardTitle } from '@/shared/components/ui/card';
import { Alert, AlertDescription } from '@/shared/components/ui/alert';

export default function RegisterPage() {
  const [email, setEmail] = useState('');
  const [password, setPassword] = useState('');
  const [confirmPassword, setConfirmPassword] = useState('');
  const [fullName, setFullName] = useState('');
  const router = useRouter();
  const searchParams = useSearchParams();
  const token = searchParams.get('token'); // قراءة رمز الدعوة من الرابط (اختياري)
  const dispatch = useAppDispatch();
  const { isAuthenticated, loading, error } = useAppSelector((state) => state.auth);
  
  // إعادة التوجيه إذا كان المستخدم مسجل الدخول بالفعل
  useEffect(() => {
    if (isAuthenticated) {
      router.push('/dashboard');
    }
  }, [isAuthenticated, router]);
  
  // تنظيف الخطأ عند التحميل
  useEffect(() => {
    dispatch(clearError());
  }, [dispatch]);
  
  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault();
    dispatch(register({ 
      email, 
      password, 
      confirm_password: confirmPassword,
      full_name: fullName,
      invitation_token: token || undefined
    }));
  };
  
  return (
    <div className="container flex items-center justify-center min-h-screen">
      <Card className="w-full max-w-md">
        <CardHeader>
          <CardTitle className="text-2xl text-center">إنشاء حساب جديد</CardTitle>
          <CardDescription className="text-center">
            قم بالتسجيل للوصول إلى لوحة تحكم المسؤولين
          </CardDescription>
        </CardHeader>
        <CardContent>
          <form onSubmit={handleSubmit} className="space-y-4">
            {error && (
              <Alert variant="destructive">
                <AlertDescription>{error}</AlertDescription>
              </Alert>
            )}
            
            <div className="space-y-2">
              <Label htmlFor="email">البريد الإلكتروني</Label>
              <Input 
                id="email" 
                type="email" 
                value={email}
                onChange={(e) => setEmail(e.target.value)}
                required
              />
            </div>
            
            <div className="space-y-2">
              <Label htmlFor="full_name">الاسم الكامل</Label>
              <Input 
                id="full_name" 
                type="text" 
                value={fullName}
                onChange={(e) => setFullName(e.target.value)}
                required
              />
            </div>
            
            <div className="space-y-2">
              <Label htmlFor="password">كلمة المرور</Label>
              <Input 
                id="password" 
                type="password" 
                value={password}
                onChange={(e) => setPassword(e.target.value)}
                required
              />
            </div>
            
            <div className="space-y-2">
              <Label htmlFor="confirm_password">تأكيد كلمة المرور</Label>
              <Input 
                id="confirm_password" 
                type="password" 
                value={confirmPassword}
                onChange={(e) => setConfirmPassword(e.target.value)}
                required
              />
            </div>
            
            <Button 
              type="submit" 
              className="w-full" 
              disabled={loading}
            >
              {loading ? 'جاري التسجيل...' : 'التسجيل'}
            </Button>
          </form>
        </CardContent>
        <CardFooter className="flex justify-center">
          <div className="text-sm text-gray-600">
            لديك حساب بالفعل؟{' '}
            <Link href="/login" className="text-blue-600 hover:text-blue-800">
              تسجيل الدخول
            </Link>
          </div>
        </CardFooter>
      </Card>
    </div>
  );
}
```

### 5.3 صفحة نسيت كلمة المرور

أنشئ ملف `src/app/forgot-password/page.tsx`:

```tsx
'use client';

import { useEffect, useState } from 'react';
import { useAppDispatch, useAppSelector } from '@/store/hooks';
import { resetPassword, clearError, clearResetEmailSent } from '@/domains/admins/store/authSlice';
import Link from 'next/link';
import { Button } from '@/shared/components/ui/button';
import { Input } from '@/shared/components/ui/input';
import { Label } from '@/shared/components/ui/label';
import { Card, CardContent, CardDescription, CardFooter, CardHeader, CardTitle } from '@/shared/components/ui/card';
import { Alert, AlertDescription } from '@/shared/components/ui/alert';

export default function ForgotPasswordPage() {
  const [email, setEmail] = useState('');
  const dispatch = useAppDispatch();
  const { loading, error, resetEmailSent } = useAppSelector((state) => state.auth);
  
  // تنظيف الحالة عند التحميل
  useEffect(() => {
    dispatch(clearError());
    dispatch(clearResetEmailSent());
  }, [dispatch]);
  
  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault();
    dispatch(resetPassword({ email }));
  };
  
  return (
    <div className="container flex items-center justify-center min-h-screen">
      <Card className="w-full max-w-md">
        <CardHeader>
          <CardTitle className="text-2xl text-center">استعادة كلمة المرور</CardTitle>
          <CardDescription className="text-center">
            أدخل بريدك الإلكتروني لإرسال رابط إعادة تعيين كلمة المرور
          </CardDescription>
        </CardHeader>
        <CardContent>
          {resetEmailSent ? (
            <Alert>
              <AlertDescription>
                تم إرسال رابط إعادة تعيين كلمة المرور إلى بريدك الإلكتروني. يرجى التحقق من بريدك الإلكتروني.
              </AlertDescription>
            </Alert>
          ) : (
            <form onSubmit={handleSubmit} className="space-y-4">
              {error && (
                <Alert variant="destructive">
                  <AlertDescription>{error}</AlertDescription>
                </Alert>
              )}
              
              <div className="space-y-2">
                <Label htmlFor="email">البريد الإلكتروني</Label>
                <Input 
                  id="email" 
                  type="email" 
                  value={email}
                  onChange={(e) => setEmail(e.target.value)}
                  required
                />
              </div>
              
              <Button 
                type="submit" 
                className="w-full" 
                disabled={loading}
              >
                {loading ? 'جاري الإرسال...' : 'إرسال رابط الاستعادة'}
              </Button>
            </form>
          )}
        </CardContent>
        <CardFooter className="flex justify-center">
          <div className="text-sm text-gray-600">
            <Link href="/login" className="text-blue-600 hover:text-blue-800">
              العودة إلى تسجيل الدخول
            </Link>
          </div>
        </CardFooter>
      </Card>
    </div>
  );
}
```

### 5.4 صفحة إعادة تعيين كلمة المرور

أنشئ ملف `src/app/reset-password/page.tsx`:

```tsx
'use client';

import { useEffect, useState } from 'react';
import { useRouter, useSearchParams } from 'next/navigation';
import { useAppDispatch, useAppSelector } from '@/store/hooks';
import { updatePassword, clearError } from '@/domains/admins/store/authSlice';
import Link from 'next/link';
import { Button } from '@/shared/components/ui/button';
import { Input } from '@/shared/components/ui/input';
import { Label } from '@/shared/components/ui/label';
import { Card, CardContent, CardDescription, CardFooter, CardHeader, CardTitle } from '@/shared/components/ui/card';
import { Alert, AlertDescription } from '@/shared/components/ui/alert';

export default function ResetPasswordPage() {
  const [password, setPassword] = useState('');
  const [confirmPassword, setConfirmPassword] = useState('');
  const [isSuccess, setIsSuccess] = useState(false);
  const router = useRouter();
  const searchParams = useSearchParams();
  const dispatch = useAppDispatch();
  const { loading, error } = useAppSelector((state) => state.auth);
  
  // التحقق من وجود رمز إعادة تعيين كلمة المرور
  useEffect(() => {
    const token = searchParams.get('token');
    if (!token) {
      router.push('/login');
    }
  }, [router, searchParams]);
  
  // تنظيف الخطأ عند التحميل
  useEffect(() => {
    dispatch(clearError());
  }, [dispatch]);
  
  const handleSubmit = async (e: React.FormEvent) => {
    e.preventDefault();
    const result = await dispatch(updatePassword({ 
      password, 
      confirm_password: confirmPassword 
    }));
    
    if (updatePassword.fulfilled.match(result)) {
      setIsSuccess(true);
      setTimeout(() => {
        router.push('/login');
      }, 3000);
    }
  };
  
  return (
    <div className="container flex items-center justify-center min-h-screen">
      <Card className="w-full max-w-md">
        <CardHeader>
          <CardTitle className="text-2xl text-center">إعادة تعيين كلمة المرور</CardTitle>
          <CardDescription className="text-center">
            أدخل كلمة المرور الجديدة
          </CardDescription>
        </CardHeader>
        <CardContent>
          {isSuccess ? (
            <Alert>
              <AlertDescription>
                تم إعادة تعيين كلمة المرور بنجاح. سيتم توجيهك إلى صفحة تسجيل الدخول...
              </AlertDescription>
            </Alert>
          ) : (
            <form onSubmit={handleSubmit} className="space-y-4">
              {error && (
                <Alert variant="destructive">
                  <AlertDescription>{error}</AlertDescription>
                </Alert>
              )}
              
              <div className="space-y-2">
                <Label htmlFor="password">كلمة المرور الجديدة</Label>
                <Input 
                  id="password" 
                  type="password" 
                  value={password}
                  onChange={(e) => setPassword(e.target.value)}
                  required
                />
              </div>
              
              <div className="space-y-2">
                <Label htmlFor="confirm_password">تأكيد كلمة المرور</Label>
                <Input 
                  id="confirm_password" 
                  type="password" 
                  value={confirmPassword}
                  onChange={(e) => setConfirmPassword(e.target.value)}
                  required
                />
              </div>
              
              <Button 
                type="submit" 
                className="w-full" 
                disabled={loading}
              >
                {loading ? 'جاري الإرسال...' : 'إعادة تعيين كلمة المرور'}
              </Button>
            </form>
          )}
        </CardContent>
        <CardFooter className="flex justify-center">
          <div className="text-sm text-gray-600">
            <Link href="/login" className="text-blue-600 hover:text-blue-800">
              العودة إلى تسجيل الدخول
            </Link>
          </div>
        </CardFooter>
      </Card>
    </div>
  );
}
```

## 6. تطبيق حراسة الطرق

### 6.1 إنشاء مكون حارس الطرق

أنشئ ملف `src/shared/components/RouteGuard.tsx`:

```tsx
'use client';

import { ReactNode, useEffect, useState } from 'react';
import { useRouter, usePathname } from 'next/navigation';
import { useAppDispatch, useAppSelector } from '@/store/hooks';
import { checkAuth } from '@/domains/admins/store/authSlice';

interface RouteGuardProps {
  children: ReactNode;
  requiredPermission?: string;
}

export function RouteGuard({ children, requiredPermission }: RouteGuardProps) {
  const router = useRouter();
  const pathname = usePathname();
  const dispatch = useAppDispatch();
  const { isAuthenticated, currentAdmin, loading } = useAppSelector((state) => state.auth);
  const [authorized, setAuthorized] = useState(false);
  
  useEffect(() => {
    // التحقق من حالة المصادقة عند بدء التشغيل
    dispatch(checkAuth());
  }, [dispatch]);
  
  useEffect(() => {
    // التحقق من المصادقة والصلاحيات
    const authCheck = async () => {
      // قائمة الطرق العامة التي لا تتطلب مصادقة
      const publicPaths = ['/login', '/register', '/forgot-password', '/reset-password'];
      const isPublicPath = publicPaths.some(path => 
        pathname === path || pathname?.startsWith(`${path}/`) || pathname?.startsWith(`${path}?`)
      );
      
      // إذا كانت الصفحة عامة، السماح بالوصول
      if (isPublicPath) {
        setAuthorized(true);
        return;
      }
      
      // التحقق من المصادقة
      if (!isAuthenticated) {
        setAuthorized(false);
        router.push('/login');
        return;
      }
      
      // التحقق من الصلاحية المطلوبة
      if (requiredPermission && currentAdmin) {
        // تحقق أولي من الصلاحيات المخزنة
        const hasPermission = currentAdmin.permissions && currentAdmin.permissions[requiredPermission];
        
        if (!hasPermission) {
          // يمكن هنا إجراء تحقق إضافي من خلال واجهة برمجة التطبيق إذا لزم الأمر
          setAuthorized(false);
          router.push('/unauthorized');
          return;
        }
      }
      
      setAuthorized(isAuthenticated);
    };
    
    authCheck();
  }, [pathname, isAuthenticated, currentAdmin, requiredPermission, router]);
  
  // عرض مؤشر التحميل أثناء التحقق من المصادقة
  if (loading) {
    return (
      <div className="flex items-center justify-center min-h-screen">
        <div className="text-center">
          <div className="w-16 h-16 border-4 border-t-blue-500 border-gray-200 rounded-full animate-spin"></div>
          <p className="mt-4">جاري التحميل...</p>
        </div>
      </div>
    );
  }
  
  // إذا لم يتم التصريح، لا تعرض أي شيء
  if (!authorized) {
    return null;
  }
  
  // إذا تم التصريح، عرض المحتوى
  return <>{children}</>;
}
```

### 6.2 إنشاء صفحة غير مصرح

أنشئ ملف `src/app/unauthorized/page.tsx`:

```tsx
'use client';

import Link from 'next/link';
import { Button } from '@/shared/components/ui/button';

export default function UnauthorizedPage() {
  return (
    <div className="container flex flex-col items-center justify-center min-h-screen">
      <h1 className="text-4xl font-bold mb-4">غير مصرح</h1>
      <p className="text-xl mb-8">
        ليس لديك الصلاحية للوصول إلى هذه الصفحة.
      </p>
      <div className="space-x-4">
        <Button asChild variant="outline">
          <Link href="/dashboard">العودة إلى لوحة التحكم</Link>
        </Button>
        <Button asChild>
          <Link href="/login">تسجيل الدخول</Link>
        </Button>
      </div>
    </div>
  );
}
```

### 6.3 تطبيق حارس الطرق في التخطيط

تحديث ملف `src/app/layout.tsx`:

```tsx
import { ReactNode } from 'react';
import { StoreProvider } from '@/core/providers/StoreProvider';
import { ThemeProvider } from '@/core/providers/ThemeProvider';
import '@/core/styles/globals.css';

interface RootLayoutProps {
  children: ReactNode;
}

export default function RootLayout({ children }: RootLayoutProps) {
  return (
    <html lang="ar" dir="rtl">
      <body>
        <StoreProvider>
          <ThemeProvider>
            {children}
          </ThemeProvider>
        </StoreProvider>
      </body>
    </html>
  );
}
```

تحديث ملف `src/app/(dashboard)/layout.tsx`:

```tsx
import { ReactNode } from 'react';
import { RouteGuard } from '@/shared/components/RouteGuard';
import { DashboardLayout } from '@/shared/layouts/DashboardLayout';

interface DashboardLayoutProps {
  children: ReactNode;
}

export default function AdminDashboardLayout({ children }: DashboardLayoutProps) {
  return (
    <RouteGuard>
      <DashboardLayout>
        {children}
      </DashboardLayout>
    </RouteGuard>
  );
}
```

## 7. ربط النظام مع إدارة الصلاحيات

### 7.1 إنشاء مكون حماية الصلاحيات

أنشئ ملف `src/domains/admins/components/PermissionGuard.tsx`:

```tsx
import { ReactNode } from 'react';
import { useAppSelector } from '@/store/hooks';

interface PermissionGuardProps {
  permissionCode: string;
  children: ReactNode;
  fallback?: ReactNode;
}

export function PermissionGuard({ permissionCode, children, fallback = null }: PermissionGuardProps) {
  const { currentAdmin } = useAppSelector((state) => state.auth);
  
  // التحقق من الصلاحية
  const hasPermission = currentAdmin?.permissions?.[permissionCode];
  
  if (!hasPermission) {
    return <>{fallback}</>;
  }
  
  return <>{children}</>;
}
```

### 7.2 إضافة زر تسجيل الخروج في القائمة

تحديث ملف `src/shared/layouts/Sidebar.tsx` أو ما يماثله:

```tsx
import { useAppDispatch } from '@/store/hooks';
import { logout } from '@/domains/admins/store/authSlice';
import { Button } from '@/shared/components/ui/button';
import { LogOut } from 'lucide-react';

// داخل مكون Sidebar
export function Sidebar() {
  const dispatch = useAppDispatch();
  
  const handleLogout = () => {
    dispatch(logout());
  };
  
  return (
    <div className="sidebar">
      {/* عناصر القائمة الأخرى... */}
      
      <div className="mt-auto p-4">
        <Button 
          variant="outline" 
          className="w-full flex items-center justify-center gap-2"
          onClick={handleLogout}
        >
          <LogOut size={16} />
          <span>تسجيل الخروج</span>
        </Button>
      </div>
    </div>
  );
}
```

### 7.3 إضافة عرض معلومات المستخدم الحالي في الرأس

تحديث ملف `src/shared/layouts/Header.tsx` أو ما يماثله:

```tsx
import { useAppSelector } from '@/store/hooks';
import {
  DropdownMenu,
  DropdownMenuContent,
  DropdownMenuItem,
  DropdownMenuLabel,
  DropdownMenuSeparator,
  DropdownMenuTrigger,
} from '@/shared/components/ui/dropdown-menu';
import { Avatar, AvatarFallback, AvatarImage } from '@/shared/components/ui/avatar';

// داخل مكون Header
export function Header() {
  const { currentAdmin } = useAppSelector((state) => state.auth);
  
  return (
    <header className="header">
      {/* محتويات أخرى... */}
      
      <div className="user-menu">
        <DropdownMenu>
          <DropdownMenuTrigger>
            <Avatar>
              {currentAdmin?.profile_image_url && (
                <AvatarImage src={currentAdmin.profile_image_url} alt={currentAdmin.full_name || ''} />
              )}
              <AvatarFallback>
                {currentAdmin?.full_name?.substring(0, 2) || currentAdmin?.username?.substring(0, 2) || 'مس'}
              </AvatarFallback>
            </Avatar>
          </DropdownMenuTrigger>
          <DropdownMenuContent align="end">
            <DropdownMenuLabel>
              {currentAdmin?.full_name || currentAdmin?.username}
            </DropdownMenuLabel>
            <DropdownMenuItem className="text-xs opacity-70">
              {currentAdmin?.email}
            </DropdownMenuItem>
            <DropdownMenuSeparator />
            <DropdownMenuItem>الملف الشخصي</DropdownMenuItem>
            <DropdownMenuItem>تغيير كلمة المرور</DropdownMenuItem>
            <DropdownMenuSeparator />
            <DropdownMenuItem className="text-red-500">تسجيل الخروج</DropdownMenuItem>
          </DropdownMenuContent>
        </DropdownMenu>
      </div>
    </header>
  );
}
```

## 8. اختبار وتحسين النظام

### 8.1 إنشاء بيانات اختبار

إنشاء ملف `src/domains/admins/data/admin-test-data.sql`:

```sql
-- إضافة أدوار للاختبار
INSERT INTO public.roles (code, name, description)
VALUES
  ('super_admin', 'مدير النظام', 'الوصول الكامل لجميع ميزات النظام'),
  ('admin', 'مسؤول', 'الوصول لمعظم ميزات النظام باستثناء الإعدادات الحساسة'),
  ('manager', 'مدير', 'إدارة العمليات اليومية ومراقبة الأداء'),
  ('supervisor', 'مشرف', 'الإشراف على مجموعة أو قسم معين'),
  ('support', 'دعم فني', 'التعامل مع استفسارات العملاء والدعم'),
  ('viewer', 'مشاهد', 'وصول للقراءة فقط')
ON CONFLICT (code) DO NOTHING;

-- إضافة مسؤول اختبار
INSERT INTO public.admins (
  user_id, 
  email, 
  username,
  full_name,
  is_active,
  role_id,
  permissions
)
VALUES (
  -- استبدل بمعرف المستخدم الخاص بك بعد التسجيل
  'USER_ID_HERE',
  'admin@example.com',
  'admin',
  'مدير النظام',
  true,
  (SELECT id FROM public.roles WHERE code = 'super_admin'),
  '{
    "dashboard:view": true,
    "admins:view": true,
    "admins:create": true,
    "admins:update": true,
    "admins:delete": true
  }'::jsonb
)
ON CONFLICT (email) DO NOTHING;
```

### 8.2 اختبار نظام المصادقة

1. **قم بتشغيل التطبيق وفتح صفحة التسجيل**
2. **قم بإنشاء حساب جديد**
3. **قم بتسجيل الدخول باستخدام البريد الإلكتروني وكلمة المرور**
4. **تحقق من استدعاء `checkAuth` وتحميل بيانات المسؤول بنجاح**
5. **قم بتسجيل الخروج واختبار إعادة التوجيه إلى صفحة تسجيل الدخول**

### 8.3 اختبار نظام الصلاحيات

1. **أنشئ صفحة مختلفة مع صلاحيات مختلفة**
2. **استخدم مكون `PermissionGuard` لإخفاء/إظهار العناصر بناءً على الصلاحيات**
3. **اختبر التنقل بين الصفحات مع حارس الطرق**

## الخطوات التالية لاستكمال التنفيذ

بعد إكمال الخطوات السابقة، يمكنك الانتقال إلى:

1. **تطوير واجهة إدارة المسؤولين**
   - صفحة لعرض وإدارة المسؤولين
   - نموذج لتحديث بيانات المسؤول
   - واجهة لإدارة الأدوار والصلاحيات

2. **تحسين أمان النظام**
   - إضافة التحقق بخطوتين (2FA)
   - إضافة سجل نشاط المسؤولين
   - تحديد فترة صلاحية الجلسة

3. **إضافة ميزات خاصة بالمؤسسة**
   - نظام دعوات للمسؤولين الجدد
   - التسلسل الهرمي للإدارة
   - نطاقات البيانات حسب القسم أو المنطقة

## الخلاصة

لقد أكملت الآن إنشاء نظام تسجيل وتسجيل دخول متكامل للمسؤولين، يعمل مع نظام الصلاحيات السابق. النظام يتبع معمارية DDD ويستخدم Redux لإدارة الحالة، مما يتوافق تمامًا مع متطلبات المشروع.

من خلال هذا الدليل، يمكنك:
- تسجيل مسؤولين جدد وتسجيل دخولهم
- التحقق من صلاحياتهم وفق الأدوار المحددة
- تأمين الطرق والمكونات في التطبيق
- تكامل نظام المصادقة مع نظام الصلاحيات الشامل

اتبع الخطوات التالية المذكورة لتطوير واجهات وميزات إضافية وفق احتياجات مشروعك.